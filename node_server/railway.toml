[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
maxRestartRetries = 3

[nixpacks]
installCommand = "npm install"
buildCommand = ""
